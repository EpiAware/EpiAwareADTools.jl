import{_ as i,o as a,c as t,an as s}from"./chunks/framework.NuKZvGF-.js";const f=JSON.parse('{"title":"User-facing opt-out: nondifferentiable","description":"","frontmatter":{},"headers":[],"relativePath":"tools/nondifferentiable.md","filePath":"tools/nondifferentiable.md","lastUpdated":null}'),n={name:"tools/nondifferentiable.md"};function o(r,e,l,d,p,c){return a(),t("div",null,[...e[0]||(e[0]=[s(`<h1 id="nondifferentiable" tabindex="-1">User-facing opt-out: <code>nondifferentiable</code> <a class="header-anchor" href="#nondifferentiable" aria-label="Permalink to &quot;User-facing opt-out: \`nondifferentiable\` {#nondifferentiable}&quot;">​</a></h1><h2 id="The-problem" tabindex="-1">The problem <a class="header-anchor" href="#The-problem" aria-label="Permalink to &quot;The problem {#The-problem}&quot;">​</a></h2><p>Part of a computation is sometimes structural rather than estimated — a grid of integration nodes, a clamp location, any fixed hyperparameter that is <em>where</em> to compute rather than <em>what</em> to estimate. <a href="/v0.2.1/lib/public#EpiAwareADTools.primal"><code>primal</code></a> and <a href="/v0.2.1/lib/public#EpiAwareADTools.primal_distribution"><code>primal_distribution</code></a> already give this discipline to the package&#39;s own internals: strip an AD wrapper to its underlying value so the quantity built from it stays off the tape. What is missing is a deliberate, user-facing entry point that applies the same discipline to a function or constructor the caller names, so intent is expressed at the call site instead of threading <code>primal</code> through downstream code by hand.</p><h2 id="The-fix" tabindex="-1">The fix <a class="header-anchor" href="#The-fix" aria-label="Permalink to &quot;The fix {#The-fix}&quot;">​</a></h2><p><code>nondifferentiable(f)</code> returns a callable that strips every argument to its primal via <a href="/v0.2.1/lib/public#EpiAwareADTools.primal"><code>primal</code></a>, calls <code>f</code> on the stripped arguments, and strips the RESULT the same way, so the call contributes exactly zero derivative on every supported backend regardless of what <code>f</code> computes internally:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> EpiAwareADTools</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">window_midpoint</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(lo, hi) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (lo </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> hi) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">/</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">frozen_midpoint </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> nondifferentiable</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(window_midpoint)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">frozen_midpoint</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;">0.5</span></span></code></pre></div><p>A struct&#39;s own constructor is itself callable, so wrapping it the same way — <code>nondifferentiable(QuadratureGrid)</code> — holds construction out of differentiation too, once the struct&#39;s own type has a <code>primal</code> method (the same pattern <code>primal_distribution</code> follows for <code>UnivariateDistribution</code>). There is deliberately no generic reflection-based <code>primal</code> fallback for an arbitrary struct: <code>isstructtype</code> is <code>true</code> for <code>Dict</code>, <code>Module</code> and every concrete function type — a closure, or <code>typeof(sin)</code> — as well as a user&#39;s own type, so a blanket fallback over every struct type would silently mishandle values this package was never asked to touch.</p><p>CAUTION — a captured value, not just an explicit argument, is also held constant: if <code>f</code> is a closure over a live differentiated value, that captured contribution is silently dropped too, consistently across every backend. This is the correct, deliberate consequence of &quot;everything in this call is a constant&quot; — never close over a value you still want differentiated.</p><p>This machinery is thin. <code>NonDifferentiable</code> and its per-backend rules are documented in full on the <a href="/v0.2.1/lib/internals#Internal-Documentation">Internal API</a> page; <code>nondifferentiable</code> itself is the public entry point.</p><h2 id="Upstream-target" tabindex="-1">Upstream target <a class="header-anchor" href="#Upstream-target" aria-label="Permalink to &quot;Upstream target {#Upstream-target}&quot;">​</a></h2><p><code>nondifferentiable</code> generalises <a href="/v0.2.1/lib/public#EpiAwareADTools.primal"><code>primal</code></a>&#39;s discipline to an arbitrary function, so it shares <code>primal</code>&#39;s target: no single owner exists for a cross-backend &quot;hold this constant&quot; primitive. This entry is deleted alongside <code>primal</code>/<code>primal_distribution</code> once a shared stop-gradient primitive exists that marks a value or a function call non-differentiable uniformly across ForwardDiff, ReverseDiff, Enzyme, and Mooncake.</p>`,12)])])}const u=i(n,[["render",o]]);export{f as __pageData,u as default};
