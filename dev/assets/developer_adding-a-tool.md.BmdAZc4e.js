import{_ as a,o as t,c as i,an as s}from"./chunks/framework.BdTLjat_.js";const k=JSON.parse('{"title":"Adding a workaround","description":"","frontmatter":{},"headers":[],"relativePath":"developer/adding-a-tool.md","filePath":"developer/adding-a-tool.md","lastUpdated":null}'),n={name:"developer/adding-a-tool.md"};function o(r,e,d,h,l,c){return t(),i("div",null,[...e[0]||(e[0]=[s(`<h1 id="adding-a-tool" tabindex="-1">Adding a workaround <a class="header-anchor" href="#adding-a-tool" aria-label="Permalink to &quot;Adding a workaround {#adding-a-tool}&quot;">​</a></h1><p>EpiAwareADTools hosts AD-safety fixes while the underlying problem is pursued upstream. Every entry is temporary by design: it carries a documented upstream target and a deletion condition, and it is removed once that upstream fix lands rather than left to rot. This page documents the contract a new entry follows and the conventions the existing entries share, using them as worked examples.</p><h2 id="The-charter-contract" tabindex="-1">The charter contract <a class="header-anchor" href="#The-charter-contract" aria-label="Permalink to &quot;The charter contract {#The-charter-contract}&quot;">​</a></h2><p>Before adding anything, check it belongs here. An entry qualifies when all of the following hold:</p><ul><li><p><strong>It is a workaround, not a feature.</strong> It stands in for a fix that ideally lives upstream (in <code>Distributions.jl</code>, <code>SpecialFunctions.jl</code>, an AD backend, or a shared primitive), not new modelling behaviour. The Gauss-Legendre quadrature layer, for example, is a feature and stays in ConvolvedDistributions.jl; see <a href="/dev/tools/index#charter">Charter and status</a>.</p></li><li><p><strong>It has a named upstream target.</strong> Link the package or issue where the fix ideally belongs.</p></li><li><p><strong>It has a deletion condition.</strong> State the observable condition under which the entry is removed (an upstream release, a merged <code>ChainRule</code>).</p></li><li><p><strong>More than one package needs it, or will.</strong> The point of a shared home is to stop the same fix being duplicated across ConvolvedDistributions.jl, CensoredDistributions.jl, and others.</p></li></ul><p>Record the entry in the <a href="/dev/tools/index#charter">Charter and status</a> table with its upstream target and deletion condition when you add it.</p><h2 id="Conventions-the-entries-share" tabindex="-1">Conventions the entries share <a class="header-anchor" href="#Conventions-the-entries-share" aria-label="Permalink to &quot;Conventions the entries share {#Conventions-the-entries-share}&quot;">​</a></h2><p><strong>Extension-point hooks.</strong> The AD-safe evaluation hooks (<code>cdf_ad_safe</code> and companions) are extension points: the generic method falls through to the stock <code>Distributions</code> function, and a bespoke method is added only for a type whose stock evaluation is not AD-safe. A new hook follows the same shape — a generic passthrough plus the narrow method that fixes the problem — so a downstream package can overload it for its own component types without inheriting a hard dependency.</p><p><strong>Per-backend rules live in extensions.</strong> Backend-specific derivative rules (ForwardDiff <code>Dual</code> methods, <code>ChainRulesCore.rrule</code>/<code>frule</code>, Enzyme and Mooncake glue) live in <code>ext/</code>, loaded only when the backend package is present. The core package stays dependency-light: the backends are <code>weakdeps</code>, and <code>src/</code> carries only the analytic machinery the rules consume (as <code>gamma_ad.jl</code> does for the shared value-and-partials helper).</p><p><strong>Centralised imports.</strong> Every module-scope <code>using</code>/<code>import</code> lives in the module file (<code>src/EpiAwareADTools.jl</code>); the extensions import the package&#39;s own internals from there. The kit&#39;s import-centralisation quality gate enforces this.</p><p><strong>AD-safe by construction.</strong> Keep non-differentiable hyperparameters off the tape with <a href="/dev/lib/public#EpiAwareADTools.primal"><code>primal</code></a>/<a href="/dev/lib/public#EpiAwareADTools.primal_distribution"><code>primal_distribution</code></a> rather than <code>try</code>/<code>catch</code> (Mooncake reverse cannot differentiate through <code>try</code>/<code>catch</code>), and select methods by dispatch.</p><h2 id="Verifying-the-new-entry" tabindex="-1">Verifying the new entry <a class="header-anchor" href="#Verifying-the-new-entry" aria-label="Permalink to &quot;Verifying the new entry {#Verifying-the-new-entry}&quot;">​</a></h2><p>Every entry is proven differentiable across the backend matrix. Register a gradient scenario in <code>test/ADFixtures/src/ADFixtures.jl</code> alongside the existing hooks:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">_push!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;my_new_hook Gamma&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    (θ, obs) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> sum</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> my_new_hook</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Gamma</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(θ[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], θ[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]), x), obs),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    [</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Constant</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(obs),))</span></span></code></pre></div><p>The AD harness in <code>test/ad/</code> then sweeps it across ForwardDiff, ReverseDiff, Enzyme (reverse and forward), and Mooncake (reverse and forward), each checked against a ForwardDiff reference gradient. Add a unit test under <code>test/unit/</code> for the primal (non-AD) behaviour, and a benchmark row under <code>benchmark/src/</code> if the entry has a hot path worth tracking.</p><h2 id="Checklist" tabindex="-1">Checklist <a class="header-anchor" href="#Checklist" aria-label="Permalink to &quot;Checklist {#Checklist}&quot;">​</a></h2><ul><li><p>[ ] Confirm it is a workaround with a named upstream target and a deletion condition</p></li><li><p>[ ] Generic passthrough plus the narrow fixing method, dispatched not branched</p></li><li><p>[ ] Per-backend rules in <code>ext/</code>, backends kept as <code>weakdeps</code>; analytic machinery in <code>src/</code></p></li><li><p>[ ] All module-scope imports in the module file (import-centralisation gate)</p></li><li><p>[ ] Export the public surface; document it in the house style (<code>@doc</code> blocks, <code># Examples</code>)</p></li><li><p>[ ] A row in the <a href="/dev/tools/index#charter">Charter and status</a> table</p></li><li><p>[ ] A gradient scenario in <code>test/ADFixtures/</code> and a unit test under <code>test/unit/</code></p></li><li><p>[ ] A benchmark row under <code>benchmark/src/</code> if it has a hot path</p></li><li><p>[ ] A tool page under <code>docs/src/tools/</code> if it is a new family</p></li></ul>`,17)])])}const u=a(n,[["render",o]]);export{k as __pageData,u as default};
