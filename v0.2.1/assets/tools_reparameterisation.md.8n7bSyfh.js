import{_ as a,o as i,c as t,an as s}from"./chunks/framework.NuKZvGF-.js";const k=JSON.parse('{"title":"Reparameterisation trick: fixed_draw / ad_eltype","description":"","frontmatter":{},"headers":[],"relativePath":"tools/reparameterisation.md","filePath":"tools/reparameterisation.md","lastUpdated":null}'),r={name:"tools/reparameterisation.md"};function n(o,e,d,l,p,h){return i(),t("div",null,[...e[0]||(e[0]=[s('<h1 id="reparameterisation" tabindex="-1">Reparameterisation trick: <code>fixed_draw</code> / <code>ad_eltype</code> <a class="header-anchor" href="#reparameterisation" aria-label="Permalink to &quot;Reparameterisation trick: `fixed_draw` / `ad_eltype` {#reparameterisation}&quot;">​</a></h1><h2 id="The-problem" tabindex="-1">The problem <a class="header-anchor" href="#The-problem" aria-label="Permalink to &quot;The problem {#The-problem}&quot;">​</a></h2><p>Taking a gradient of a quantity computed from random draws only makes sense if the draws are held fixed and only the parameters vary; otherwise the derivative is meaningless rather than merely noisy — the reparameterisation-trick, or common-random-numbers, discipline. The package already owns the mirror-image half of it: <a href="/v0.2.1/lib/public#EpiAwareADTools.primal"><code>primal</code></a> and <a href="/v0.2.1/lib/public#EpiAwareADTools.primal_distribution"><code>primal_distribution</code></a> strip genuinely non-differentiable values off the tape while real parameters flow through. The complementary half — deliberately pinning a set of draws as fixed realisations while promoting every parameter-dependent accumulator to the differentiated element type — was previously folk knowledge, re-implemented ad hoc wherever the pattern appears. See <a href="https://github.com/EpiAware/CensoredDistributions.jl/pull/824" target="_blank" rel="noreferrer">EpiAware/CensoredDistributions.jl#824</a> for the motivating context this pair grew out of.</p><p>Both moves are easy to get subtly wrong by hand. A draw that silently moves with the parameters, rather than staying fixed, makes the gradient simply wrong. And an accumulator seeded at a plain <code>Float64</code> <code>zero</code>, or a buffer preallocated at <code>Float64</code>, works only until a differentiated term (a ForwardDiff <code>Dual</code>, a ReverseDiff <code>TrackedReal</code>) is combined with it. At that point writing the wrapper into a concretely-<code>Float64</code>-typed slot raises a <code>MethodError</code>.</p><h2 id="The-fix" tabindex="-1">The fix <a class="header-anchor" href="#The-fix" aria-label="Permalink to &quot;The fix {#The-fix}&quot;">​</a></h2><p><a href="/v0.2.1/lib/public#EpiAwareADTools.fixed_draw"><code>fixed_draw</code></a> pins a draw as a fixed, non-differentiated realisation by delegating to <a href="/v0.2.1/lib/public#EpiAwareADTools.primal"><code>primal</code></a>: the identical strip mechanism, given a distinct name because the value frozen here is a REALISED DRAW rather than a structural hyperparameter. Because it delegates directly, the existing per-backend <code>primal</code> marks (<code>@non_differentiable</code>, <code>EnzymeRules.inactive</code>, <code>@zero_derivative</code>) already cover it, so no new per-backend extension code is needed.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> EpiAwareADTools</span></span>\n<span class="line"></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">z </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> fixed_draw</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rand</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))</span></span>\n<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">length</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(z)</span></span></code></pre></div><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;">3</span></span></code></pre></div><p><code>fixed_draw</code> only guards against an AD wrapper that survives the sampling step itself; it does NOT by itself implement the common-random-numbers discipline. The draw must already be independent of the differentiated parameters — generated from their <code>primal</code> values, or before the parameters vary at all — or the resulting gradient is wrong rather than merely imprecise.</p><p><a href="/v0.2.1/lib/public#EpiAwareADTools.ad_eltype"><code>ad_eltype</code></a> resolves the type a parameter-dependent accumulator should be seeded at, mirroring <code>primal</code>&#39;s own <code>Real</code>/<code>Tuple</code>/<code>AbstractArray</code> dispatch: <code>typeof(x)</code> for a plain real, and the <code>promote_type</code> across a container&#39;s elements otherwise. Mixing a differentiated parameter alongside a <code>fixed_draw</code> constant in the same container still resolves to the wider type:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">T </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> ad_eltype</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">total </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> sum</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">fixed_draw</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.([</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.9</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]); init </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> zero</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(T))</span></span></code></pre></div><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;">1.4</span></span></code></pre></div><p>Enzyme and Mooncake trace plain <code>Float64</code> code by source transformation rather than threading a wrapper type through the primal computation, so <code>ad_eltype</code> is a harmless no-op — always <code>Float64</code> — on those two backends. It does the real work on ForwardDiff and ReverseDiff, where the differentiated parameter genuinely carries a different runtime type. The full docstrings, including the buffer-seeding worked example, live on the <a href="/v0.2.1/lib/public#public-api">Public API</a> page.</p><h2 id="Upstream-target" tabindex="-1">Upstream target <a class="header-anchor" href="#Upstream-target" aria-label="Permalink to &quot;Upstream target {#Upstream-target}&quot;">​</a></h2><p><code>fixed_draw</code> shares <a href="/v0.2.1/lib/public#EpiAwareADTools.primal"><code>primal</code></a>&#39;s target exactly, since it delegates to it directly: no single owner exists for a cross-backend &quot;hold this constant&quot; primitive. <code>ad_eltype</code> has no upstream owner either: no shared AD-abstraction layer currently exposes a backend-agnostic &quot;what type should a differentiated accumulator be&quot; query. Both entries are deleted once such primitives exist: <code>fixed_draw</code> alongside <code>primal</code>/<code>primal_distribution</code>, and <code>ad_eltype</code> once, for example, <code>DifferentiationInterface.jl</code> (or an equivalent shared abstraction) gains a backend-agnostic differentiated-eltype query.</p>',15)])])}const m=a(r,[["render",n]]);export{k as __pageData,m as default};
