import{_ as a,o as t,c as o,an as r}from"./chunks/framework.BB5DBKdI.js";const f=JSON.parse('{"title":"Beta-CDF derivative","description":"","frontmatter":{},"headers":[],"relativePath":"tools/beta-cdf.md","filePath":"tools/beta-cdf.md","lastUpdated":null}'),d={name:"tools/beta-cdf.md"};function i(c,e,n,s,l,h){return t(),o("div",null,[...e[0]||(e[0]=[r('<h1 id="beta-cdf" tabindex="-1">Beta-CDF derivative <a class="header-anchor" href="#beta-cdf" aria-label="Permalink to &quot;Beta-CDF derivative {#beta-cdf}&quot;">​</a></h1><h2 id="The-problem" tabindex="-1">The problem <a class="header-anchor" href="#The-problem" aria-label="Permalink to &quot;The problem {#The-problem}&quot;">​</a></h2><p>The regularised incomplete beta <code>I_x(α, β)</code> is the Beta CDF, and <code>SpecialFunctions.beta_inc</code> computes it accurately across every regime. Its <code>ChainRule</code>, though, leaves the partials with respect to the shape parameters <code>α</code> and <code>β</code> unimplemented. A Beta CDF differentiated in its shape therefore has no derivative on any ChainRules-based backend, and the forward-mode <code>Dual</code> and Enzyme paths hit the same wall — the <code>beta_inc</code> analogue of the <a href="/previews/PR65/tools/gamma-cdf#gamma-cdf">Gamma-CDF derivative</a> gap.</p><p>This also blocks any distribution whose CDF routes through <code>beta_inc</code>, not just <code>Beta</code> itself — Student-t and LogLogistic among them.</p><h2 id="The-fix" tabindex="-1">The fix <a class="header-anchor" href="#The-fix" aria-label="Permalink to &quot;The fix {#The-fix}&quot;">​</a></h2><p><code>_beta_cdf(α, β, x)</code> evaluates <code>I_x(α, β)</code> on the primal path through <code>beta_inc</code>, and the extensions supply the derivative analytically:</p><ul><li><p><code>_rib_value_and_partials</code> computes the missing shape partials by term-by-term differentiation of the continued-fraction form Boik &amp; Robinson-Cox (1998) derive for <code>I_x(p, q)</code>, evaluated with the standard modified Lentz-style recurrence, and ported from the reference C implementation in Caner Türkmen&#39;s <a href="https://github.com/canerturkmen/betaincder" target="_blank" rel="noreferrer">betaincder</a>.</p></li><li><p><code>_beta_cdf_value_and_partials</code> returns the primal and the <code>(dα, dβ, dx)</code> partials from one helper, applying the reflection symmetry <code>I_x(α,β) = 1 - I_{1-x}(β,α)</code> when <code>x &gt; α/(α+β)</code> (the continued fraction is only valid on the other side), so the ChainRules rule, the ForwardDiff <code>Dual</code> methods, and the Enzyme rule share the same formulas.</p></li><li><p><code>EpiAwareADToolsChainRulesCoreExt</code> defines the reverse-mode <code>rrule</code> and forward-mode <code>frule</code>; <code>EpiAwareADToolsReverseDiffExt</code> and <code>EpiAwareADToolsMooncakeExt</code> lift these into their backends; <code>EpiAwareADToolsForwardDiffExt</code> adds <code>Dual</code> methods directly; and <code>EpiAwareADToolsEnzymeExt</code> adds a direct Enzyme rule.</p></li></ul><p><code>ad_safe.jl</code>&#39;s <code>pdf_ad_safe(::Beta)</code> override is a separate, narrower fix in the same family: it routes around a confirmed-wrong Enzyme rule for <code>LogExpFunctions.xlog1py</code>&#39;s first argument (the stock <code>pdf(::Beta)</code> path) by computing the density directly from <code>log</code>/<code>log1p</code>, rather than reaching for <code>_beta_cdf</code>.</p><p>This machinery is internal. Consumers reach it through the <a href="/previews/PR65/tools/ad-safe-hooks#ad-safe-hooks">AD-safe hooks</a>; the <a href="/previews/PR65/lib/internals#Internal-Documentation">Internal API</a> page carries the full docstrings.</p><h2 id="Upstream-target" tabindex="-1">Upstream target <a class="header-anchor" href="#Upstream-target" aria-label="Permalink to &quot;Upstream target {#Upstream-target}&quot;">​</a></h2><p>The whole family stands in for a differentiable <code>beta_inc</code>. There is no tracking issue open against <code>SpecialFunctions.jl</code> for the shape-parameter partials at the time of writing (unlike <code>gamma_inc</code>&#39;s <a href="https://github.com/JuliaMath/SpecialFunctions.jl/issues/531" target="_blank" rel="noreferrer">issue #531</a>). Once <code>beta_inc</code> carries a complete <code>ChainRule</code>, <code>_beta_cdf</code> and its rules are deleted and the hooks route through the stock CDF.</p>',11)])])}const m=a(d,[["render",i]]);export{f as __pageData,m as default};
