import{_ as s,o as a,c as i,an as t}from"./chunks/framework.CbxhlEWH.js";const g=JSON.parse('{"title":"Streaming log-sum-exp: logsumexp_stream","description":"","frontmatter":{},"headers":[],"relativePath":"tools/logsumexp-stream.md","filePath":"tools/logsumexp-stream.md","lastUpdated":null}'),n={name:"tools/logsumexp-stream.md"};function r(l,e,o,h,p,d){return a(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="logsumexp-stream" tabindex="-1">Streaming log-sum-exp: <code>logsumexp_stream</code> <a class="header-anchor" href="#logsumexp-stream" aria-label="Permalink to &quot;Streaming log-sum-exp: \`logsumexp_stream\` {#logsumexp-stream}&quot;">​</a></h1><h2 id="The-problem" tabindex="-1">The problem <a class="header-anchor" href="#The-problem" aria-label="Permalink to &quot;The problem {#The-problem}&quot;">​</a></h2><p>The package&#39;s other shared primitives (the tape-strip pair and the AD-safe evaluation hooks) all target continuous quadrature over a distribution&#39;s CDF/PDF. There is no shared primitive for the other recurring shape: an infinite sum in log space over an unbounded discrete support. Every consumer that needs one currently writes its own stopping rule, and the easy mistake is stopping at the first negligible term — which silently truncates a heavy tail that dips low for a term or two and then recovers, biasing the result with no visible signal.</p><h2 id="The-fix" tabindex="-1">The fix <a class="header-anchor" href="#The-fix" aria-label="Permalink to &quot;The fix {#The-fix}&quot;">​</a></h2><p><code>logsumexp_stream(log_term)</code> computes <code>log(Σ_{k≥0} exp(log_term(k)))</code> without materialising the full term sequence. It accumulates with the classic running maximum/rescale log-sum-exp identity, updated incrementally as each <code>log_term(k)</code> arrives, and stops only once the running total has been unchanged within <code>atol</code> for <code>min_stable_terms</code> CONSECUTIVE further terms — not at the first negligible one:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> EpiAwareADTools</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Σ_{k≥0} exp(-k), a geometric series with ratio exp(-1); the exact closed</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># form is 1 / (1 - exp(-1)), so log of that is the reference value.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">result </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> logsumexp_stream</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(k </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Float64</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(k))</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">result</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">value </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">≈</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> log</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> /</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> exp</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))), result</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">converged</span></span></code></pre></div><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;">(true, true)</span></span></code></pre></div><p>It returns <code>(value, terms_used, converged)</code>. When <code>max_terms</code> is reached without stabilising, the default (<code>strict = true</code>) raises a descriptive error rather than silently returning a partial sum; <code>strict = false</code> instead returns the partial result with <code>converged = false</code>.</p><p>The accumulator is plain generic Julia control flow — comparisons, <code>exp</code>, <code>log</code>, <code>+</code>, <code>*</code> — over whatever real (or AD-wrapped) values <code>log_term</code> returns, calling no non-differentiable primitive anywhere in the body. Every supported AD backend (ForwardDiff, ReverseDiff, Enzyme, Mooncake, ChainRulesCore) differentiates straight through <code>value</code> with no bespoke per-backend rule needed, the same way a plain <code>sum(logpdf.(...))</code> loop does. The full docstring, including the numerical trap the three-way step branch guards against, lives on the <a href="/v0.2.2/lib/public#public-api">Public API</a> page.</p><h2 id="Upstream-target" tabindex="-1">Upstream target <a class="header-anchor" href="#Upstream-target" aria-label="Permalink to &quot;Upstream target {#Upstream-target}&quot;">​</a></h2><p>There is no single upstream owner for a differentiable, convergence-checked streaming log-sum-exp over an unbounded support. <code>LogExpFunctions.jl</code> hosts the ecosystem&#39;s <code>logsumexp</code>, but only over an already-materialised, finite collection — not a term generator with a run-based stopping rule. This entry is deleted once <code>LogExpFunctions.jl</code> (or an equivalent shared numerics package) gains a differentiable streaming accumulator with the same convergence guarantee.</p>`,11)])])}const u=s(n,[["render",r]]);export{g as __pageData,u as default};
