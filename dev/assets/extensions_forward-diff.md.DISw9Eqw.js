import{_ as s,o as i,c as e,an as t}from"./chunks/framework.D1f4kYqw.js";const k=JSON.parse('{"title":"ForwardDiff extension","description":"","frontmatter":{},"headers":[],"relativePath":"extensions/forward-diff.md","filePath":"extensions/forward-diff.md","lastUpdated":null}'),h={name:"extensions/forward-diff.md"};function d(l,a,o,n,r,p){return i(),e("div",null,[...a[0]||(a[0]=[t(`<h1 id="ext-forward-diff" tabindex="-1">ForwardDiff extension <a class="header-anchor" href="#ext-forward-diff" aria-label="Permalink to &quot;ForwardDiff extension {#ext-forward-diff}&quot;">​</a></h1><p><code>EpiAwareADToolsForwardDiffExt</code> loads when <code>ForwardDiff</code> is loaded alongside <code>EpiAwareADTools</code>. ForwardDiff dispatches on the <code>Dual</code> argument type and does not read ChainRules, so this extension adds methods directly rather than lifting the rules the <a href="/dev/extensions/chain-rules-core#ext-chain-rules-core">ChainRulesCore extension</a> defines.</p><h2 id="What-it-registers" tabindex="-1">What it registers <a class="header-anchor" href="#What-it-registers" aria-label="Permalink to &quot;What it registers {#What-it-registers}&quot;">​</a></h2><p>A tape-strip method:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primal</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Dual</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> primal</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">value</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x))</span></span></code></pre></div><p>It recurses, so a nested <code>Dual</code> from a higher-order tag chain still reduces to the scalar value. <a href="/dev/lib/public#EpiAwareADTools.fixed_draw"><code>fixed_draw</code></a> delegates to <a href="/dev/lib/public#EpiAwareADTools.primal"><code>primal</code></a>, so it becomes a genuine stop-gradient on this backend through the same method.</p><p>Seven methods each for <code>_gamma_cdf</code>, <code>_gamma_logccdf</code> and <code>_beta_cdf</code>, covering every combination of the three arguments in which at least one is a <code>Dual</code>. All of them route to a shared <code>_impl</code> helper that reads out the values, calls the matching <code>_value_and_partials</code> helper, and rebuilds a <code>Dual</code> from the analytic partials and the incoming ones. <code>Dual &lt;: Real</code>, so the seven methods overlap, and the three double-<code>Dual</code> methods and the all-<code>Dual</code> method are the resolvers for those overlaps. The <code>Dual</code> slots are deliberately left unparametrised. A shared <code>{T, V, N}</code> parametrisation across slots would pin the tags equal, the resolvers would then not dominate the mixed-tag intersections, and <code>detect_ambiguities</code> would flag every partial pair. The tag and partial width are read from the first <code>Dual</code> at run time instead.</p><p>Two edge cases are unsupported and error inside the helper rather than through ambiguous dispatch: nested <code>Dual</code>s, and mixed tags across arguments. ForwardDiff never asks for either in a single differentiation pass.</p><p>Twelve <code>Distributions</code> methods, six for <code>Gamma</code> and six for <code>Beta</code>:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Distributions</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">logcdf</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Gamma{&lt;:Dual}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, x</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Real</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> logcdf_ad_safe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(d, x)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Distributions</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">logcdf</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Gamma</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, x</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Dual</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> logcdf_ad_safe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(d, x)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Distributions</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">logcdf</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Gamma{&lt;:Dual}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, x</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Dual</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> logcdf_ad_safe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(d, x)</span></span></code></pre></div><p>and the <code>logccdf</code> counterparts, then the same six for <code>Beta</code>. Methods are added only for the <code>Dual</code> arguments <code>StatsFuns</code> cannot handle, so the plain float path is untouched. The <code>Gamma{&lt;:Dual}</code> method catches <code>Dual</code> parameters with a constant evaluation point, the <code>::Dual</code> evaluation-point method catches a <code>Dual</code> bound, and the both-<code>Dual</code> method resolves their overlap.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> EpiAwareADTools, Distributions, ForwardDiff</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ForwardDiff</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">derivative</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(k </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> logcdf</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Gamma</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(k, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1.5</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;">-0.4949622662152675</span></span></code></pre></div><h2 id="What-fails-without-it" tabindex="-1">What fails without it <a class="header-anchor" href="#What-fails-without-it" aria-label="Permalink to &quot;What fails without it {#What-fails-without-it}&quot;">​</a></h2><p><code>StatsFuns._gammalogcdf</code> has no <code>Dual</code> method, and neither does the <code>beta_inc</code> path behind <code>logcdf(::Beta)</code>. A <code>truncated(Gamma; lower)</code> builds its normaliser eagerly at construction, so constructing one with <code>Dual</code> parameters breaks under ForwardDiff before any density is evaluated. The <code>logcdf</code>/<code>logccdf</code> methods above are what close that gap, by routing through <a href="/dev/lib/public#EpiAwareADTools.logcdf_ad_safe"><code>logcdf_ad_safe</code></a> and <a href="/dev/lib/public#EpiAwareADTools.logccdf_ad_safe"><code>logccdf_ad_safe</code></a> and so through the analytic shape and scale partials.</p><p>Without the <code>Dual</code> methods on the CDF primitives, the <a href="/dev/tools/ad-safe-hooks#ad-safe-hooks">AD-safe hooks</a> themselves have nothing differentiable to call, since the generic primitives return a plain float and drop the partials.</p><p>Without the <code>primal(::Dual)</code> method, <code>primal</code> is the identity on a <code>Dual</code>, so a strip a caller relied on silently does nothing and the gradient flows on through.</p><h2 id="Upstream-target" tabindex="-1">Upstream target <a class="header-anchor" href="#Upstream-target" aria-label="Permalink to &quot;Upstream target {#Upstream-target}&quot;">​</a></h2><p>The same condition as the rest of the family: a <code>Gamma</code> and <code>Beta</code> CDF differentiable in their parameters upstream, tracked for the gamma side by <a href="https://github.com/JuliaMath/SpecialFunctions.jl/issues/531" target="_blank" rel="noreferrer">SpecialFunctions.jl issue #531</a>. The <code>primal(::Dual)</code> method goes with the rest of the tape-strip once a shared cross-backend stop-gradient primitive exists.</p>`,19)])])}const g=s(h,[["render",d]]);export{k as __pageData,g as default};
