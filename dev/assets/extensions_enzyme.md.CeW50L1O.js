import{_ as a,o as i,c as s,an as t}from"./chunks/framework.D1f4kYqw.js";const k=JSON.parse('{"title":"Enzyme extension","description":"","frontmatter":{},"headers":[],"relativePath":"extensions/enzyme.md","filePath":"extensions/enzyme.md","lastUpdated":null}'),n={name:"extensions/enzyme.md"};function o(r,e,d,h,l,c){return i(),s("div",null,[...e[0]||(e[0]=[t(`<h1 id="ext-enzyme" tabindex="-1">Enzyme extension <a class="header-anchor" href="#ext-enzyme" aria-label="Permalink to &quot;Enzyme extension {#ext-enzyme}&quot;">​</a></h1><p><code>EpiAwareADToolsEnzymeExt</code> loads when <code>Enzyme</code> is loaded alongside <code>EpiAwareADTools</code>. It is the only extension that writes its rules directly against a backend&#39;s own rule interface rather than lifting ChainRules, because Enzyme does not read ChainRules rules.</p><h2 id="What-it-registers" tabindex="-1">What it registers <a class="header-anchor" href="#What-it-registers" aria-label="Permalink to &quot;What it registers {#What-it-registers}&quot;">​</a></h2><p>Two inactivity marks:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">EnzymeRules</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">inactive</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">typeof</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(primal), args</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> nothing</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">EnzymeRules</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">inactive</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">NonDifferentiable</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, args</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> nothing</span></span></code></pre></div><p>The first keeps the <a href="/dev/tools/tape-strip#tape-strip">tape-strip</a> constant: Enzyme runs the primal call unchanged and contributes no tangent and no cotangent. <code>inactive</code> covers every activity, batch-width and mode permutation at once. The second gives the same treatment to any instance of the <a href="/dev/tools/nondifferentiable#nondifferentiable"><code>nondifferentiable</code></a> wrapper. Dispatch matches on the wrapper&#39;s own unparametrised type, so one registration covers every wrapped function, closures included.</p><p>Three <code>EnzymeRules.@easy_rule</code> registrations, one per internal CDF primitive:</p><ul><li><p><code>_gamma_cdf(k, θ, x)</code>, with <code>(dk, dθ, dx)</code> from <code>_gamma_cdf_value_and_partials</code>.</p></li><li><p><code>_gamma_logccdf(k, θ, x)</code>, from <code>_gamma_logccdf_value_and_partials</code>.</p></li><li><p><code>_beta_cdf(α, β, x)</code>, from <code>_beta_cdf_value_and_partials</code>.</p></li></ul><p>Each macro expands into the reverse-mode <code>augmented_primal</code>/<code>reverse</code> pair and the forward-mode <code>forward</code> rule together. The partials come from the same helpers the <a href="/dev/extensions/chain-rules-core#ext-chain-rules-core">ChainRulesCore extension</a> uses, so the formulas are shared rather than restated.</p><p>One rule for a function this package does not own:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">EnzymeRules</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@easy_rule</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">gamma</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Real</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), (Ω </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> digamma</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x),))</span></span></code></pre></div><h2 id="What-fails-without-it" tabindex="-1">What fails without it <a class="header-anchor" href="#What-fails-without-it" aria-label="Permalink to &quot;What fails without it {#What-fails-without-it}&quot;">​</a></h2><p>Routing the CDF primitives through explicit rules is what stops Enzyme differentiating <code>SpecialFunctions.gamma_inc</code> and <code>beta_inc</code> directly. Those have no shape derivative to find, so an unloaded extension turns a Gamma or Beta CDF differentiated in its parameters into a failed or wrong gradient.</p><p>The <code>gamma</code> rule fixes a separate, quieter problem. Enzyme&#39;s own <code>EnzymeSpecialFunctionsExt</code> ships no <code>gamma</code> rule and mis-lowers <code>gamma(x)</code> to the <code>loggamma</code> known operation, returning <code>ψ(x)</code> where the derivative is <code>Γ(x) ψ(x)</code>. That is wrong by a factor of <code>Γ(x)</code> in both modes, and it is an upstream Enzyme bug. It reaches this package because <code>_gamma_cdf_value_and_partials</code> calls <code>pdf(Gamma(...))</code>, which uses <code>gamma</code> outside the <code>_gamma_cdf</code> rule. The <code>_beta_cdf</code> path is not exposed to it, since Enzyme never traces into that helper.</p><p>Without the two <code>inactive</code> marks, Enzyme differentiates through <code>primal</code> and through a wrapped <code>nondifferentiable</code> call, which is exactly what both exist to prevent. As on the other backends, an inactive call also zeroes the contribution of a value a wrapped closure captured rather than received as an argument.</p><h2 id="Upstream-target" tabindex="-1">Upstream target <a class="header-anchor" href="#Upstream-target" aria-label="Permalink to &quot;Upstream target {#Upstream-target}&quot;">​</a></h2><p>The three CDF rules are deleted once <code>SpecialFunctions.jl</code> carries complete <code>ChainRule</code>s for <code>gamma_inc</code> (<a href="https://github.com/JuliaMath/SpecialFunctions.jl/issues/531" target="_blank" rel="noreferrer">issue #531</a>) and <code>beta_inc</code> and Enzyme picks up correct derivatives for them. The <code>gamma</code> rule is deleted once Enzyme stops lowering <code>gamma</code> through <code>loggamma</code>. The two <code>inactive</code> marks go once a shared cross-backend stop-gradient primitive exists.</p>`,17)])])}const m=a(n,[["render",o]]);export{k as __pageData,m as default};
