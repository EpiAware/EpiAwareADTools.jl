# managed:start
# MANAGED by EpiAwarePackageTools.scaffold — do not edit between
# the markers. Add a reformat commit's SHA after the closing
# marker — it is preserved across updates.
# Formatting-only commits (e.g. a Runic layout pass) touch every line without
# changing behaviour, burying the real last-change commit under `git blame`.
# List such a commit's SHA below this block to have it skipped. Configure git
# to use this file once, locally:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
# managed:end

# style: reformat the package-owned source with Runic
164c5118bb8a5972563cb7eb4bb1b56fe5b2bd49
