import{_ as a,o as t,c as o,an as i}from"./chunks/framework.D1f4kYqw.js";const f=JSON.parse('{"title":"ChainRulesCore extension","description":"","frontmatter":{},"headers":[],"relativePath":"extensions/chain-rules-core.md","filePath":"extensions/chain-rules-core.md","lastUpdated":null}'),r={name:"extensions/chain-rules-core.md"};function n(s,e,d,c,l,h){return t(),o("div",null,[...e[0]||(e[0]=[i('<h1 id="ext-chain-rules-core" tabindex="-1">ChainRulesCore extension <a class="header-anchor" href="#ext-chain-rules-core" aria-label="Permalink to &quot;ChainRulesCore extension {#ext-chain-rules-core}&quot;">​</a></h1><p><code>EpiAwareADToolsChainRulesCoreExt</code> loads when <code>ChainRulesCore</code> is loaded alongside <code>EpiAwareADTools</code>. It is the extension the others build on. <code>ReverseDiff</code> and <code>Mooncake</code> both depend on <code>ChainRulesCore</code>, so loading either of those backends loads this extension as well.</p><h2 id="What-it-registers" tabindex="-1">What it registers <a class="header-anchor" href="#What-it-registers" aria-label="Permalink to &quot;What it registers {#What-it-registers}&quot;">​</a></h2><p>Two non-differentiability marks:</p><ul><li><p><code>@non_differentiable primal(::Any)</code>, so a reverse-mode backend that reads ChainRules stops at the <a href="/dev/tools/tape-strip#tape-strip">tape-strip</a> instead of tracing through it.</p></li><li><p><code>@non_differentiable (nd::NonDifferentiable)(args...)</code>, the same mark for every instance of the <a href="/dev/tools/nondifferentiable#nondifferentiable"><code>nondifferentiable</code></a> wrapper. It is registered once on the wrapper type rather than once per wrapped function. The wrapper&#39;s own body already strips its arguments and its result through <a href="/dev/lib/public#EpiAwareADTools.primal"><code>primal</code></a>, so this mark is a second line of defence for a ChainRules-consuming context beyond the four backends this package tests directly.</p></li></ul><p>An <code>rrule</code> and an <code>frule</code> for each of the three internal CDF primitives:</p><ul><li><p><code>_gamma_cdf(k, θ, x) = P(k, x/θ)</code>, the <a href="/dev/tools/gamma-cdf#gamma-cdf">gamma-CDF derivative</a>.</p></li><li><p><code>_gamma_logccdf(k, θ, x) = log(Q(k, x/θ))</code>, its log-space survival companion.</p></li><li><p><code>_beta_cdf(α, β, x) = I_x(α, β)</code>, the <a href="/dev/tools/beta-cdf#beta-cdf">beta-CDF derivative</a>.</p></li></ul><p>Each rule reads the primal value and the three partials from the matching <code>_value_and_partials</code> helper in <code>src/gamma_ad.jl</code> or <code>src/beta_ad.jl</code>. The helpers, not the rules, hold the analytic formulas. The ForwardDiff <code>Dual</code> methods and the Enzyme rules call the same helpers, so no backend carries its own copy of the maths.</p><p>The <code>frule</code> matters as much as the <code>rrule</code>. Mooncake&#39;s forward mode is generated by lifting ChainRules, and a lift with no <code>frule</code> behind it calls <code>ChainRulesCore.frule</code>, gets <code>nothing</code> back, and fails with <code>iterate(::Nothing)</code>.</p><h2 id="What-fails-without-it" tabindex="-1">What fails without it <a class="header-anchor" href="#What-fails-without-it" aria-label="Permalink to &quot;What fails without it {#What-fails-without-it}&quot;">​</a></h2><p>Only these rules and their per-backend counterparts register a derivative for the incomplete gamma and beta functions. With the extension unloaded, a ChainRules-based backend differentiating a Gamma or Beta CDF reaches <code>SpecialFunctions</code>&#39; own rules, whose shape partials are <code>@not_implemented</code>, and the gradient call errors. The <a href="/dev/extensions/reverse-diff#ext-reverse-diff">ReverseDiff</a> and <a href="/dev/extensions/mooncake#ext-mooncake">Mooncake</a> extensions lift the rules defined here, so without this extension they have nothing to lift.</p><h2 id="Upstream-target" tabindex="-1">Upstream target <a class="header-anchor" href="#Upstream-target" aria-label="Permalink to &quot;Upstream target {#Upstream-target}&quot;">​</a></h2><p><code>SpecialFunctions.jl</code> carrying complete <code>ChainRule</code>s for <code>gamma_inc</code> (<a href="https://github.com/JuliaMath/SpecialFunctions.jl/issues/531" target="_blank" rel="noreferrer">issue #531</a>) and for <code>beta_inc</code>. The three pairs of rules are deleted when that lands, along with the internal primitives they cover. The two <code>@non_differentiable</code> marks outlive them, and go once a shared cross-backend stop-gradient primitive exists.</p>',13)])])}const u=a(r,[["render",n]]);export{f as __pageData,u as default};
