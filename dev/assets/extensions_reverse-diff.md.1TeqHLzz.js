import{_ as a,o as i,c as s,an as t}from"./chunks/framework.D1f4kYqw.js";const k=JSON.parse('{"title":"ReverseDiff extension","description":"","frontmatter":{},"headers":[],"relativePath":"extensions/reverse-diff.md","filePath":"extensions/reverse-diff.md","lastUpdated":null}'),r={name:"extensions/reverse-diff.md"};function n(o,e,d,h,l,c){return i(),s("div",null,[...e[0]||(e[0]=[t('<h1 id="ext-reverse-diff" tabindex="-1">ReverseDiff extension <a class="header-anchor" href="#ext-reverse-diff" aria-label="Permalink to &quot;ReverseDiff extension {#ext-reverse-diff}&quot;">​</a></h1><p><code>EpiAwareADToolsReverseDiffExt</code> loads when <code>ReverseDiff</code> is loaded alongside <code>EpiAwareADTools</code>. ReverseDiff depends on <code>ChainRulesCore</code>, so loading it also loads the <a href="/dev/extensions/chain-rules-core#ext-chain-rules-core">ChainRulesCore extension</a>, whose <code>rrule</code>s this extension lifts onto the tape.</p><h2 id="What-it-registers" tabindex="-1">What it registers <a class="header-anchor" href="#What-it-registers" aria-label="Permalink to &quot;What it registers {#What-it-registers}&quot;">​</a></h2><p>A tape-strip method:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">primal</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">TrackedReal</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> primal</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ReverseDiff</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">value</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x))</span></span></code></pre></div><p>Reading <code>.value</code> off a tape entry records no operation, so a value stripped this way stays a constant. That is the behaviour a non-differentiable hyperparameter wants. <a href="/dev/lib/public#EpiAwareADTools.fixed_draw"><code>fixed_draw</code></a> delegates to <a href="/dev/lib/public#EpiAwareADTools.primal"><code>primal</code></a> and so inherits it.</p><p>Twenty-one <code>@grad_from_chainrules</code> registrations: seven each for <code>_gamma_cdf</code>, <code>_gamma_logccdf</code> and <code>_beta_cdf</code>, one for every non-trivial subset of tracked and untracked arguments, for example</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@grad_from_chainrules</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> _gamma_cdf</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(k</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">TrackedReal</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, θ</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Real</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, x</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Real</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>The full seven per primitive are needed because <code>@grad_from_chainrules</code> is signature-specific rather than abstract, so a mixed call that no registration matches falls through.</p><h2 id="What-fails-without-it" tabindex="-1">What fails without it <a class="header-anchor" href="#What-fails-without-it" aria-label="Permalink to &quot;What fails without it {#What-fails-without-it}&quot;">​</a></h2><p>The lifted rules are what put the analytic partials on the tape. Without them ReverseDiff either falls back to forward mode through <code>gamma_inc</code> or <code>beta_inc</code>, which have no <code>TrackedReal</code> method and so error, or, depending on the call site, traces the function body instead. Tracing is slower than calling the analytic <code>rrule</code> directly even in the cases where it does produce a number, and it still cannot supply the shape partial <code>SpecialFunctions</code> leaves <code>@not_implemented</code>.</p><p>Without the <code>primal(::TrackedReal)</code> method, <code>primal</code> is the identity on a tracked value. The value stays on the tape, and a quantity the caller intended as a constant keeps contributing a gradient.</p><h2 id="Upstream-target" tabindex="-1">Upstream target <a class="header-anchor" href="#Upstream-target" aria-label="Permalink to &quot;Upstream target {#Upstream-target}&quot;">​</a></h2><p><code>SpecialFunctions.jl</code> carrying complete <code>ChainRule</code>s for <code>gamma_inc</code> (<a href="https://github.com/JuliaMath/SpecialFunctions.jl/issues/531" target="_blank" rel="noreferrer">issue #531</a>) and <code>beta_inc</code>, at which point the primitives and their lifts are all deleted. The <code>primal(::TrackedReal)</code> method goes with the rest of the tape-strip once a shared cross-backend stop-gradient primitive exists.</p>',14)])])}const f=a(r,[["render",n]]);export{k as __pageData,f as default};
