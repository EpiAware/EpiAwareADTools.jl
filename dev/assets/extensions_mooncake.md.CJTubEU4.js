import{_ as a,o as t,c as i,an as s}from"./chunks/framework.D1f4kYqw.js";const k=JSON.parse('{"title":"ChainRulesCore + Mooncake extension","description":"","frontmatter":{},"headers":[],"relativePath":"extensions/mooncake.md","filePath":"extensions/mooncake.md","lastUpdated":null}'),o={name:"extensions/mooncake.md"};function n(r,e,d,h,l,c){return t(),i("div",null,[...e[0]||(e[0]=[s(`<h1 id="ext-mooncake" tabindex="-1">ChainRulesCore + Mooncake extension <a class="header-anchor" href="#ext-mooncake" aria-label="Permalink to &quot;ChainRulesCore + Mooncake extension {#ext-mooncake}&quot;">​</a></h1><p><code>EpiAwareADToolsMooncakeExt</code> loads when <code>ChainRulesCore</code> and <code>Mooncake</code> are both loaded alongside <code>EpiAwareADTools</code>. Mooncake depends on <code>ChainRulesCore</code> today, so in practice loading Mooncake is enough, but the rules this extension lifts are defined by the <a href="/dev/extensions/chain-rules-core#ext-chain-rules-core">ChainRulesCore extension</a> and that extension fires on <code>ChainRulesCore</code>. Naming both triggers keeps the two in step if Mooncake ever drops the dependency.</p><h2 id="What-it-registers" tabindex="-1">What it registers <a class="header-anchor" href="#What-it-registers" aria-label="Permalink to &quot;What it registers {#What-it-registers}&quot;">​</a></h2><p>Three lifts of the ChainRules rules into Mooncake&#39;s rule registry, one per internal CDF primitive:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Mooncake</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@from_chainrules</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Mooncake</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">DefaultCtx Tuple{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    typeof</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(_gamma_cdf), Real, Real, Real,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>and the same for <code>_gamma_logccdf</code> and <code>_beta_cdf</code>. The default mode generates both an <code>rrule!!</code> and an <code>frule!!</code>, so forward and reverse Mooncake are covered by one registration. The signatures are abstract <code>Real</code> triples rather than concrete floats, so a call that mixes types, such as <code>_gamma_cdf(k + 1, θ, t)</code> with a <code>Float32</code> parameter or a <code>BigFloat</code> used for a higher-precision check, still hits the explicit rule instead of falling back to Mooncake tracing the function body.</p><p>Two zero-derivative registrations:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Mooncake</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@zero_derivative</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Mooncake</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">DefaultCtx Tuple{</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">typeof</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(primal), Real}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Mooncake</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@zero_derivative</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Mooncake</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">DefaultCtx Tuple{NonDifferentiable, Vararg}</span></span></code></pre></div><p>The first keeps the <a href="/dev/tools/tape-strip#tape-strip">tape-strip</a> off the AD path. The second does the same for every instance of the <a href="/dev/tools/nondifferentiable#nondifferentiable"><code>nondifferentiable</code></a> wrapper. The bare, unparametrised pattern means one registration covers any wrapped function, a closure included, whatever its own type. Neither macro takes a mode argument, so both forward and reverse mode get a zero rule.</p><h2 id="What-fails-without-it" tabindex="-1">What fails without it <a class="header-anchor" href="#What-fails-without-it" aria-label="Permalink to &quot;What fails without it {#What-fails-without-it}&quot;">​</a></h2><p>Mooncake does not lift the ChainRules <code>@non_differentiable primal</code> mark automatically. Without the <code>@zero_derivative</code> registration it traces <code>primal</code> as the identity and can propagate a gradient the caller meant to cut, silently.</p><p>For the CDF primitives, an unloaded extension leaves Mooncake tracing into <code>gamma_inc</code> and <code>beta_inc</code>. Reverse mode is left without the shape partial that <code>SpecialFunctions</code> marks <code>@not_implemented</code>. Forward mode fails harder: the generated <code>frule!!</code> calls <code>ChainRulesCore.frule</code>, gets <code>nothing</code> for an undefined rule, and errors with <code>iterate(::Nothing)</code>.</p><p>Wrapping a function with <code>nondifferentiable</code> and differentiating it under Mooncake also has a consequence worth stating. A wrapped closure that captures a live differentiated value, rather than receiving it as an argument, has that captured contribution silently zeroed too. This is the same behaviour as every other supported backend, and it is confirmed directly on both Mooncake modes.</p><p>Note that the <code>xlogy</code> and <code>xlog1py</code> rules are not here. They live in the separate <a href="/dev/extensions/log-exp-functions-mooncake#ext-log-exp-functions-mooncake">ChainRulesCore + LogExpFunctions + Mooncake extension</a>, which needs <code>LogExpFunctions</code> as a third trigger.</p><h2 id="Upstream-target" tabindex="-1">Upstream target <a class="header-anchor" href="#Upstream-target" aria-label="Permalink to &quot;Upstream target {#Upstream-target}&quot;">​</a></h2><p>The three lifts are deleted when <code>SpecialFunctions.jl</code> carries complete <code>ChainRule</code>s for <code>gamma_inc</code> (<a href="https://github.com/JuliaMath/SpecialFunctions.jl/issues/531" target="_blank" rel="noreferrer">issue #531</a>) and <code>beta_inc</code>, since the primitives they cover go with them. The two zero-derivative rules go once a shared cross-backend stop-gradient primitive exists.</p>`,16)])])}const u=a(o,[["render",n]]);export{k as __pageData,u as default};
