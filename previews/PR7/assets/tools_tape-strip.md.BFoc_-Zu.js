import{_ as a,o as i,c as t,an as s}from"./chunks/framework.qxY8nuy4.js";const m=JSON.parse('{"title":"Tape-strip: primal","description":"","frontmatter":{},"headers":[],"relativePath":"tools/tape-strip.md","filePath":"tools/tape-strip.md","lastUpdated":null}'),r={name:"tools/tape-strip.md"};function o(n,e,l,p,d,h){return i(),t("div",null,[...e[0]||(e[0]=[s('<h1 id="tape-strip" tabindex="-1">Tape-strip: <code>primal</code> <a class="header-anchor" href="#tape-strip" aria-label="Permalink to &quot;Tape-strip: `primal` {#tape-strip}&quot;">​</a></h1><h2 id="The-problem" tabindex="-1">The problem <a class="header-anchor" href="#The-problem" aria-label="Permalink to &quot;The problem {#The-problem}&quot;">​</a></h2><p>A numeric routine sometimes needs a value that is a function of the model parameters but must not carry a gradient. The usual example is a quadrature window: the endpoint at which an infinite integration limit is clamped is a function of the component parameters, but it is a hyperparameter of the quadrature — just <em>where</em> to integrate — and it should not be differentiated. If live <code>Dual</code> or <code>TrackedReal</code> parameters flow into the code that picks the endpoint, the AD backend traces functions it cannot handle (for example <code>gamma_inc_inv</code> for a <code>Gamma</code> component under Enzyme) and the differentiation fails or returns <code>NaN</code>.</p><h2 id="The-fix" tabindex="-1">The fix <a class="header-anchor" href="#The-fix" aria-label="Permalink to &quot;The fix {#The-fix}&quot;">​</a></h2><p><a href="/EpiAwareADTools.jl/previews/PR7/lib/public#EpiAwareADTools.primal"><code>primal</code></a> reduces an AD-wrapped scalar to its underlying value, and <a href="/EpiAwareADTools.jl/previews/PR7/lib/public#EpiAwareADTools.primal_distribution"><code>primal_distribution</code></a> rebuilds a distribution from primal parameters. A quantity computed from primal parameters is a constant as far as AD is concerned, so it stays off the tape.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> EpiAwareADTools, Distributions</span></span>\n<span class="line"></span>\n<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">primal</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">primal_distribution</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Gamma</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1.5</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))</span></span></code></pre></div><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;">(3.0, Distributions.Gamma{Float64}(α=2.0, θ=1.5))</span></span></code></pre></div><h2 id="Per-backend-behaviour" tabindex="-1">Per-backend behaviour <a class="header-anchor" href="#Per-backend-behaviour" aria-label="Permalink to &quot;Per-backend behaviour {#Per-backend-behaviour}&quot;">​</a></h2><p>The generic <code>primal</code> is the identity on a plain real, so a non-AD call keeps the value&#39;s own type. The extensions make it a genuine stop-gradient on each backend:</p><ul><li><p><code>EpiAwareADToolsForwardDiffExt</code> and <code>EpiAwareADToolsReverseDiffExt</code> add methods that read the underlying value out of a <code>Dual</code> or <code>TrackedReal</code>.</p></li><li><p><code>EpiAwareADToolsChainRulesCoreExt</code> marks <code>primal</code> <code>@non_differentiable</code>.</p></li><li><p><code>EpiAwareADToolsEnzymeExt</code> marks it <code>EnzymeRules.inactive</code>.</p></li><li><p><code>EpiAwareADToolsMooncakeExt</code> gives it a <code>@zero_derivative</code> rule.</p></li></ul><h2 id="Upstream-target" tabindex="-1">Upstream target <a class="header-anchor" href="#Upstream-target" aria-label="Permalink to &quot;Upstream target {#Upstream-target}&quot;">​</a></h2><p>There is no single upstream owner for a cross-backend stop-gradient. This pair is deleted once a shared primitive exists that marks a value non-differentiable uniformly across ForwardDiff, ReverseDiff, Enzyme, and Mooncake. Until then hosting one sanctioned <code>primal</code> here is better than each consumer package carrying its own underscore-prefixed copy.</p>',12)])])}const u=a(r,[["render",o]]);export{m as __pageData,u as default};
