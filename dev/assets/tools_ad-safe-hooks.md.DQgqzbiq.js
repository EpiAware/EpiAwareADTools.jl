import{_ as a,o as s,c as i,an as t}from"./chunks/framework.D1f4kYqw.js";const k=JSON.parse('{"title":"AD-safe evaluation hooks","description":"","frontmatter":{},"headers":[],"relativePath":"tools/ad-safe-hooks.md","filePath":"tools/ad-safe-hooks.md","lastUpdated":null}'),d={name:"tools/ad-safe-hooks.md"};function o(n,e,l,h,c,r){return s(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="ad-safe-hooks" tabindex="-1">AD-safe evaluation hooks <a class="header-anchor" href="#ad-safe-hooks" aria-label="Permalink to &quot;AD-safe evaluation hooks {#ad-safe-hooks}&quot;">​</a></h1><h2 id="The-problem" tabindex="-1">The problem <a class="header-anchor" href="#The-problem" aria-label="Permalink to &quot;The problem {#The-problem}&quot;">​</a></h2><p>The stock <code>Distributions.jl</code> evaluators for a <code>Gamma</code> or a <code>Beta</code> are not differentiable in the distribution&#39;s shape (and, for <code>Gamma</code>, scale). <code>cdf(::Gamma)</code> routes through <code>SpecialFunctions.gamma_inc</code> and <code>cdf(::Beta)</code> through <code>SpecialFunctions.beta_inc</code>; both leave their shape partials unimplemented in their <code>ChainRule</code>s, and the corresponding <code>logcdf</code>/<code>logccdf</code> paths (<code>StatsFuns._gammalogcdf</code>/<code>_gammalogccdf</code> and the <code>beta_inc</code>-routed <code>Beta</code> equivalents) have no <code>Dual</code> shape method either. A downstream numeric kernel that evaluates a Gamma or Beta component&#39;s CDF, survival, or density inside a differentiated integrand therefore breaks under every supported AD backend.</p><h2 id="The-fix" tabindex="-1">The fix <a class="header-anchor" href="#The-fix" aria-label="Permalink to &quot;The fix {#The-fix}&quot;">​</a></h2><p>Five hooks give a wrapper package one sanctioned surface to evaluate a component through:</p><ul><li><p><a href="/dev/lib/public#EpiAwareADTools.cdf_ad_safe"><code>cdf_ad_safe</code></a></p></li><li><p><a href="/dev/lib/public#EpiAwareADTools.logcdf_ad_safe"><code>logcdf_ad_safe</code></a></p></li><li><p><a href="/dev/lib/public#EpiAwareADTools.ccdf_ad_safe"><code>ccdf_ad_safe</code></a></p></li><li><p><a href="/dev/lib/public#EpiAwareADTools.logccdf_ad_safe"><code>logccdf_ad_safe</code></a></p></li><li><p><a href="/dev/lib/public#EpiAwareADTools.pdf_ad_safe"><code>pdf_ad_safe</code></a></p></li></ul><p>Each is an extension point. The generic method falls through to the stock <code>Distributions</code> function, so a distribution that already differentiates cleanly is untouched. It carries no type bound on the distribution argument, so a leaf that implements the univariate interface through the <code>Distributions.jl</code> generic functions, without subtyping <code>UnivariateDistribution</code>, is evaluated the same way. The <code>Gamma</code> methods route through the <a href="/dev/tools/gamma-cdf#gamma-cdf">gamma-CDF derivative</a>, and the <code>Beta</code> methods through the <a href="/dev/tools/beta-cdf#beta-cdf">beta-CDF derivative</a>, so each stays differentiable in its parameters.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> EpiAwareADTools, Distributions</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Gamma</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cdf_ad_safe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(d, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">logccdf_ad_safe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(d, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">db </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Beta</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cdf_ad_safe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(db, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">logccdf_ad_safe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(db, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;">(0.08999999999999998, -0.0943106794712413)</span></span></code></pre></div><h2 id="t-hooks" tabindex="-1">Student-t <a class="header-anchor" href="#t-hooks" aria-label="Permalink to &quot;Student-t {#t-hooks}&quot;">​</a></h2><p>A Student-t CDF is the regularised incomplete beta at <code>ν / (ν + x^2)</code>, so <code>logcdf(::TDist)</code> reaches <code>beta_inc</code> through <code>StatsFuns.tdistlogcdf</code> and inherits the <code>Beta</code> problem above. It bites earlier than the <code>Gamma</code> and <code>Beta</code> cases, because <code>truncated</code> normalises eagerly: <code>truncated(TDist(ν), l, u)</code> calls <code>logcdf</code> at construction, before any density is evaluated, so a truncated t could not be built at all under AD (EpiAwareADTools#80). <code>tdistlogcdf</code> is typed <code>(ν::T, x::T)</code>, so it promotes an untouched degrees of freedom to the AD type and the break lands even when only a location and scale are being differentiated.</p><p>The <code>TDist</code> methods route through the internal <code>_t_cdf</code> family, which composes over the <a href="/dev/tools/beta-cdf#beta-cdf">beta-CDF derivative</a> rather than carrying rules of its own, and there are matching methods for the location-scale wrapper <code>μ + σ * TDist(ν)</code>.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">dt </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0.3</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> +</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1.4</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> TDist</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">5.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cdf_ad_safe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(dt, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">logccdf_ad_safe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(dt, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;">(0.6808505641795356, -1.1420958350382855)</span></span></code></pre></div><p>Loading <code>ForwardDiff</code> additionally claims <code>Distributions.logcdf</code>/<code>logccdf</code> on <code>Dual</code> arguments for <code>TDist</code>, the same way the extension already does for <code>Gamma</code> and <code>Beta</code>, so <code>truncated</code> builds and a gradient flows straight through the stock call. The wrapper needs no methods of its own there: the stock affine <code>logcdf</code> standardises the evaluation point and delegates to the inner t, so a <code>Dual</code> location or scale arrives as a <code>Dual</code> point and a <code>Dual</code> degrees of freedom as a <code>TDist{&lt;:Dual}</code>. The other backends never surface a wrapper type to the primal computation, so they reach the same machinery through the hooks instead.</p><div class="tip custom-block"><p class="custom-block-title">Far tail</p><p><code>logcdf_ad_safe</code> and <code>logccdf_ad_safe</code> on a Student-t always evaluate the smaller of the two tails and never reconstruct it as <code>1 - F</code>. At <code>ν = 5</code> and <code>x = -1e8</code> the value agrees with the incomplete beta&#39;s own small-argument asymptote to nine significant figures, where the stock evaluator has already lost all but the first.</p></div><h2 id="Extending-the-hooks" tabindex="-1">Extending the hooks <a class="header-anchor" href="#Extending-the-hooks" aria-label="Permalink to &quot;Extending the hooks {#Extending-the-hooks}&quot;">​</a></h2><p>A wrapper package adds a method for its own component type. For example, a package whose modified density is not AD-safe under a given backend adds a <code>pdf_ad_safe</code> method for its type, and the numeric kernel that calls <code>pdf_ad_safe</code> then differentiates cleanly:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> EpiAwareADTools</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> pdf_ad_safe</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pdf_ad_safe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MyModifiedDist</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, t</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Real</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> my_ad_safe_density</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(d, t)</span></span></code></pre></div><h2 id="survival-hooks" tabindex="-1">Survival distributions <a class="header-anchor" href="#survival-hooks" aria-label="Permalink to &quot;Survival distributions {#survival-hooks}&quot;">​</a></h2><p>Loading <code>SurvivalDistributions.jl</code> alongside this package adds hook methods for <code>SurvivalDistributions.GeneralizedGamma</code>, supplied by the <code>EpiAwareADToolsSurvivalDistributionsExt</code> extension.</p><p>A <code>GeneralizedGamma(σ, ν, γ)</code> carries an inner <code>Gamma(ν/γ, σ^γ)</code> and defines its survival as <code>logccdf(d, t) = logccdf(d.G, t^γ)</code>, so its CDF family inherits exactly the <code>Gamma</code> problem above: any kernel differentiating through a GeneralizedGamma leaf reaches <code>StatsFuns._gammalogccdf</code> and errors on every backend. The extension routes the inner Gamma through <a href="/dev/lib/public#EpiAwareADTools.cdf_ad_safe"><code>cdf_ad_safe</code></a> at the transformed point <code>t^γ</code>. The transform and the inner shape/scale are elementary, so the gradient flows through all three parameters.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> SurvivalDistributions</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">dg </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> GeneralizedGamma</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1.5</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1.3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cdf_ad_safe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(dg, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">logccdf_ad_safe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(dg, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;">(0.8143002202039338, -1.6836239964288853)</span></span></code></pre></div><p>The extension also claims <code>Distributions.logcdf(::GeneralizedGamma, ::Real)</code>. <code>SurvivalDistributions</code> defines <code>logccdf</code> but no <code>logcdf</code>, so a bare <code>logcdf</code> call otherwise falls through to the generic <code>logcdf(d, x) = log(cdf(d, x))</code>, and from there to <code>SurvivalDistributions</code>&#39; own <code>cdf(GG, t) = 1 - exp(logccdf(d.G, t^γ))</code> — so it lands back on the non-differentiable <code>_gammalogccdf</code> too. <code>cdf</code>, <code>ccdf</code>, and <code>logccdf</code> are owned by <code>SurvivalDistributions</code> and are left alone; redefining them would be method-overwriting piracy.</p><p><code>SurvivalDistributions.LogLogistic</code> needs no methods: its evaluators are built from elementary operations and differentiate through the generic fallback.</p><div class="tip custom-block"><p class="custom-block-title">Far right tail</p><p><code>logccdf_ad_safe</code> on a <code>Gamma</code> (and so, transitively, on a <code>GeneralizedGamma</code>) routes through <code>_gamma_logccdf</code> (see the <a href="/dev/tools/gamma-cdf#gamma-cdf">Gamma-CDF derivative</a> page), which reads the survival directly from <code>SpecialFunctions.gamma_inc</code> rather than reconstructing it as <code>1 - F</code>. Its <em>value</em> therefore tracks the stock <code>logccdf</code> at implementation tolerance across the whole domain, including deep into the right tail where <code>F</code> itself has already rounded to <code>1</code> (EpiAwareADTools#47). The <em>gradient</em> stays finite and accurate to arbitrary tail depth as well. The <code>x</code> and <code>θ</code> partials reduce to the hazard-type ratio <code>f/Q</code> and are formed in log space as <code>exp(logpdf - logccdf)</code>, which never underflows. The shape partial divides the exact series by <code>gamma_inc</code>&#39;s accurately-computed survival while that is at least <code>√eps</code> of the working float type, and switches to a corrected asymptotic series beyond, holding a relative error of about <code>1e-6</code> or better at every depth in Float64.</p></div><h2 id="Upstream-target" tabindex="-1">Upstream target <a class="header-anchor" href="#Upstream-target" aria-label="Permalink to &quot;Upstream target {#Upstream-target}&quot;">​</a></h2><p>The family exists because the Gamma and Beta CDFs are not differentiable in their parameters upstream — the same gaps the <a href="/dev/tools/gamma-cdf#gamma-cdf">gamma-CDF derivative</a> (<a href="https://github.com/JuliaMath/SpecialFunctions.jl/issues/531" target="_blank" rel="noreferrer">SpecialFunctions.jl issue #531</a>) and the <a href="/dev/tools/beta-cdf#beta-cdf">beta-CDF derivative</a> (no tracking issue yet) fill. The hooks are deleted once a Gamma CDF and a Beta CDF differentiable in their parameters ship upstream and wrapper packages can call <code>cdf</code>/<code>logcdf</code> directly.</p>`,29)])])}const g=a(d,[["render",o]]);export{k as __pageData,g as default};
